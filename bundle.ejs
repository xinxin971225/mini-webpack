!(function (modules) {
  function require(filePath) {

    const fn = modules[filePath];
    const module = {
      exports: {},
    };
    fn(require, module, module.exports);
    return module.exports;
  }

  require("./main.js");
})({
  <% data.forEach(data=>{ %>
  "<%- data['filePath'] %>": function (require, module, exports) {
       <%- data['code'] %>
  },
  <% })%>
});
